# Hive Metastore (Lakehouse)

This folder holds optional Hive Metastore configuration notes for lakehouse connectivity.

- Active Hive config for the `data-lab` container is in:
  `datalabcontainer/dev/hive/conf/hive-site.xml`
- Default metastore endpoint used by Trino:
  `thrift://localhost:9083`
