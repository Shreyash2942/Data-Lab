# dev/mongodb

Reference Dockerfile for MongoDB binaries used in Data Lab.

- Base: `ubuntu:22.04`
- Installs: MongoDB community binaries under `/opt/mongodb`

Main runtime still uses `dev/base/Dockerfile` in the single-container setup.
