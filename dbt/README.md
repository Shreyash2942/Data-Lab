# Dbt Layer

dbt project files used from inside the monolithic container. A ready-to-use DuckDB profile lives in `dbt/profiles.yml`, pointing to `~/dbt/data_lab.duckdb`.

## Resources
- Official docs: https://docs.getdbt.com/
