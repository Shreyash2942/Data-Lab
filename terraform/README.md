# Terraform Layer

Optional Terraform configuration run from inside the monolithic container. Terraform downloads/plugins/state are stored in `~/runtime/terraform`, so deleting that folder resets the demo safely.

## Resources
- Official docs: https://developer.hashicorp.com/terraform/docs
