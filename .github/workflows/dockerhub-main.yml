name: Build and Push Data-Lab Image

on:
  push:
    branches:
      - main
<<<<<<< HEAD
=======
    paths:
      - ".github/workflows/dockerhub-main.yml"
      - ".dockerignore"
      - "datalabcontainer/dev/base/**"
      - "datalabcontainer/dev/hadoop/conf/**"
      - "datalabcontainer/dev/hive/conf/**"
      - "datalabcontainer/dev/kafka/conf/**"
      - "datalabcontainer/dev/spark/conf/**"
      - "datalabcontainer/app/**"
      - "datalabcontainer/runtime/README.md"
      - "stacks/**"
>>>>>>> dev
  workflow_dispatch:

permissions:
  contents: read

<<<<<<< HEAD
=======
concurrency:
  group: dockerhub-main-${{ github.ref }}
  cancel-in-progress: true

>>>>>>> dev
jobs:
  dockerhub:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Resolve Dockerfile path
        id: resolve
        shell: bash
        run: |
          if [ -f "datalabcontainer/dev/base/Dockerfile" ]; then
            echo "dockerfile=datalabcontainer/dev/base/Dockerfile" >> "$GITHUB_OUTPUT"
            echo "context=." >> "$GITHUB_OUTPUT"
          elif [ -f "dev/base/Dockerfile" ]; then
            echo "dockerfile=dev/base/Dockerfile" >> "$GITHUB_OUTPUT"
            echo "context=." >> "$GITHUB_OUTPUT"
          else
            echo "No supported Dockerfile found." >&2
            exit 1
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/data-lab
          tags: |
            type=raw,value=latest
            type=sha,format=short

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ${{ steps.resolve.outputs.context }}
          file: ${{ steps.resolve.outputs.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
<<<<<<< HEAD
          cache-from: type=gha
          cache-to: type=gha,mode=max
=======
          cache-from: |
            type=gha,scope=dockerhub-main
            type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/data-lab:buildcache
          cache-to: |
            type=gha,mode=max,scope=dockerhub-main
            type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/data-lab:buildcache,mode=max
          provenance: false
>>>>>>> dev
